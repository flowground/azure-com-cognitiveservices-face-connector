{
    "type": "object",
    "properties": {
        "faceListId": {
            "required": true,
            "type": "string",
            "maxLength": 64,
            "pattern": "^[a-z0-9-_]+$"
        },
        "persistedFaceId": {
            "required": true,
            "type": "string",
            "format": "uuid"
        }
    }
}