{
    "type": "object",
    "properties": {
        "personGroupId": {
            "required": true,
            "type": "string",
            "maxLength": 64,
            "pattern": "^[a-z0-9-_]+$"
        }
    }
}